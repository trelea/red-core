import React from 'react';

/**
 * Admin login logo (replaces the default Payload logo) — the full Red Core logo.
 */
export const Logo = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img src="/logo.svg" alt="Red Core" style={{ height: 42, width: 'auto' }} />
);
